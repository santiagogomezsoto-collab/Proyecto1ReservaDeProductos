/**
 * Punto de entrada de la app. Va a configurar el Look & Feel, mostrar el
 * login (JDialog bloqueante) y, si el login fue exitoso, armar la ventana
 * principal (JFrame + JTabbedPane) con las pestañas segun el rol del usuario.
 *
 * TODO: implementar main(), doLogin() y doRun() (ver estructura_proyecto_reservas.md).
 * Por ahora esta vacia, cada quien la va completando cuando su parte este lista.
 */
public class Application {
}
